<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".benchmark.BenchmarkDataReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mochisofts.mata.action.SEED_BENCHMARK_DATA" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
