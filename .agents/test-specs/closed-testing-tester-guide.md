# MATA Closed testing参加ガイド

- 対象: MATAの初回Closed testingへ参加するテスター
- 対象版: `1.0.0 (1)`。変更時は配布連絡を正とする
- 問い合わせ・フィードバック: `com.mochisofts@gmail.com`
- 運営用台帳: [初回Closed testing実施台帳](closed-testing-log.md)
- 担当試験: [初回リリース試験担当割り当て](closed-testing-assignments.md)

## 1. お願いしたいこと

MATAは、毎日、曜日指定、毎月、一定期間内の回数指定等の繰り返しTODOを管理するAndroidアプリである。テストでは、実際に数日間利用し、基本操作、繰り返し、通知、ウィジェット、バックアップ等が期待どおり動くかを確認する。

今回のテストでは、Google Playの本番アクセス要件を満たすため、案内した期間中はテストへの参加状態を継続する。途中で参加解除が必要になった場合は、事前または解除後に運営者へ連絡する。

## 2. 参加前の準備

- Android 8.0（API 26）以上の端末を使用する。
- 招待対象として登録されたGoogleアカウントを、端末のGoogle Playで利用できる状態にする。
- Google グループ方式で案内された場合は、先に指定されたグループへ同じGoogleアカウントで参加する。
- Internal testingへ参加中のアカウントは、Internal testingから参加解除した後にClosed testingへ参加する。
- 既存データを使わず、個人情報を含まないテスト専用のTODO名・説明・カテゴリ名を使用する。

テスト版は通常のGoogle Play検索で見つからない場合があるため、必ず運営者から届いた参加URLを使用する。初回公開や更新の反映には数時間かかる場合がある。

## 3. 参加・インストール手順

1. 端末のGoogle Playで、招待に使用したGoogleアカウントを選択する。
2. Google グループ方式の場合は、同じアカウントで指定グループへ参加する。
3. 運営者から共有されたClosed testingの参加URLを開く。
4. Google Playの案内に従ってテスターとして参加する。
5. 表示されたGoogle PlayのページからMATAをインストールする。
6. MATAを起動し、TODO一覧が表示されることを確認する。
7. 設定画面の「バージョン」が配布連絡の対象版と一致することを確認する。
8. 運営者から割り当てられたテスターID、端末名、Androidバージョン、インストール結果を報告する。

参加URLは公開場所へ転載しない。Google Playで参加操作が完了していても、アプリが表示されるまで時間がかかる場合がある。

## 4. 最初に確認する基本操作

可能な範囲で次を順に試す。運営者から個別の担当範囲が指定された場合は、そちらを優先する。

1. カテゴリを登録・編集し、色とアイコンを変更する。
2. 単発TODOを登録し、今日の一覧へ表示されることを確認する。
3. TODOを完了し、カレンダーの履歴へ反映されることを確認する。
4. 別のTODOをスキップし、一覧と履歴が正しく更新されることを確認する。
5. 毎日または曜日指定の繰り返しTODOを登録し、予定日へ表示されることを確認する。
6. アプリを終了・再起動し、TODO、カテゴリ、履歴、設定が残っていることを確認する。
7. ハンバーガーメニューから各画面を開き、戻る操作と画面遷移を確認する。

## 5. 担当者だけが確認する操作

次の項目は、必要な権限、時刻待ち、ホーム画面変更またはファイル操作を伴う。運営者から依頼された場合だけ実施する。

- 通知権限の許可・拒否、事前通知、期限通知、期限超過通知
- ホーム画面へのウィジェット追加、完了・スキップ、アプリ本体との表示同期
- 一日の終了時刻前後、日付・週・月の境界、タイムゾーン変更
- 手動バックアップの作成と復元
- 画面回転、分割画面、最大フォント、表示サイズ拡大、ダークテーマ
- TalkBackを使った読み上げ順序と操作
- オフラインでの基本操作とオンライン復帰

バックアップファイルには入力したTODO等が含まれる。ファイル自体を運営者へ送らず、必ずテスト専用データで確認する。

## 6. テスト期間中の利用

- 運営者が終了を案内するまで、Closed testingの参加状態を解除しない。
- アプリを一度起動するだけで終わらせず、複数の日にTODOの登録、完了またはスキップを行う。
- Google Playに更新が表示された場合は更新し、更新前後のデータが維持されるか確認する。
- クラッシュ、応答停止、データ消失、意図しない通知、ウィジェットの不整合は優先して報告する。
- 広告が表示されてもクリックしない。表示崩れや操作との重なりだけを確認する。

## 7. フィードバック方法

Google Playの非公開フィードバック、または`com.mochisofts@gmail.com`へ送信する。テスト版へ送ったGoogle Playのフィードバックは公開レビューにはならない。

### 7.1 問題がなかった場合

次の形式で報告する。

```text
件名: [MATA Closed Test][Txx] 動作確認結果

テスターID: Txx
確認日: YYYY-MM-DD
端末名:
Androidバージョン:
MATAバージョン:
確認した機能:
結果: 問題なし
気になった点: なし／内容
```

### 7.2 不具合または分かりにくい点があった場合

```text
件名: [MATA Closed Test][Txx] 不具合・改善提案

テスターID: Txx
発生日時: YYYY-MM-DD HH:mmごろ
端末名:
Androidバージョン:
MATAバージョン:
対象画面・機能:
発生前の状態:
行った操作:
期待した結果:
実際の結果:
再現頻度: 毎回／時々／1回だけ
アプリ再起動後: 改善／継続／未確認
添付: なし／スクリーンショット／画面録画
```

画面が操作できない、データが消えた、通知が繰り返し誤発火する等の重大な問題は、件名に`[緊急]`を加えて早めに報告する。

## 8. 添付時の注意

- スクリーンショットや動画では、通知、TODO内容、メールアドレス、アカウント画像等の個人情報を隠す。
- 実際の予定や秘密情報ではなく、「テストTODO 1」のような再現用データを使う。
- バックアップファイル、端末全体のバグレポート、Googleアカウント情報は送信しない。
- 診断情報の追加提出が必要な場合は、運営者から対象と取得手順を個別に案内する。

## 9. 参加できないとき

### 参加ページを開けない

- 招待対象と同じGoogleアカウントで開いているか確認する。
- Google グループ方式の場合は、先にグループへ参加しているか確認する。
- アプリまたはリリースがGoogle Playで公開状態になるまで数時間待ち、再度開く。

### Internal testing版が表示される

Internal testingから参加解除し、その後Closed testingの参加URLから入り直す。Google Playへの反映後、Closed testing版へ更新する。

### インストールまたは更新が表示されない

- Google Playアプリを閉じて開き直す。
- 端末上のGoogleアカウントを確認する。
- 数時間待って再確認する。
- 改善しない場合は、表示されたメッセージのスクリーンショットを個人情報を隠して送る。

## 10. テスト終了

運営者から終了の案内が届くまでは参加状態を維持する。終了案内後は、必要に応じてテスト参加を解除し、MATAをアンインストールできる。参加解除後も、インストール済みアプリが自動的に端末から削除されるわけではない。

## 11. 運営者向け配布前チェック

- [ ] Closed testingリリースが`公開済み`である。
- [ ] テスターのGoogleアカウントまたはGoogle グループが対象へ登録されている。
- [ ] フィードバック先に`com.mochisofts@gmail.com`を設定した。
- [ ] 参加URLとGoogle Play URLをリポジトリ外の連絡経路で共有する。
- [ ] 各テスターへ`T01`〜`T12`のIDと担当範囲を割り当てた。
- [ ] [初回Closed testing実施台帳](closed-testing-log.md)へ参加日、端末、OSを記録する。
- [ ] 参加人数と継続日数をPlay Consoleで定期確認する。

## 12. 配布メッセージひな型

`[]`内を置き換えてテスターへ送る。参加URLはこのリポジトリへコミットしない。

```text
Androidアプリ「MATA」のClosed testingへのご協力をお願いします。

あなたのテスターID: [Txx]
確認してほしい期間: [YYYY-MM-DD]から終了連絡まで
Google グループ: [利用する場合のみURL]
テスト参加URL: [Play Consoleの参加URL]
担当してほしい機能: [担当範囲]

参加URLは、招待に使用したGoogleアカウントで開いてください。
期間中はテスト参加を解除せず、複数の日にアプリを利用してください。
詳しい手順と報告方法は添付の「MATA Closed testing参加ガイド」を確認してください。

不具合や気になった点は com.mochisofts@gmail.com へお送りください。
スクリーンショットを送る場合は、個人情報や実際のTODO内容を隠してください。
本番広告が表示されてもクリックしないでください。
```

## 13. 参考資料

- [Google Play Console ヘルプ: Set up an open, closed, or internal test](https://support.google.com/googleplay/android-developer/answer/9845334)
- [Google Play Console ヘルプ: App testing requirements for new personal developer accounts](https://support.google.com/googleplay/android-developer/answer/14151465)
